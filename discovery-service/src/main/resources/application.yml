spring:
  application:
    name: server

server:
  port: 19090

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://loclahost:19090/eureka/
  instance:
    hostname: localhost
